# Deploy to Firebase Hosting on merge
# (auto-generated by Firebase CLI, edited to inject Supabase env vars)

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches: [main, deploy-test]

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
<<<<<<< HEAD
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
=======
      SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
>>>>>>> 7c367af29eecec437e53fe38cfa7f84e3ddef9b0
    steps:
      - uses: actions/checkout@v4

      - name: Build web app
        run: cd frontend/web && npm ci && npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_Q2CHAT }}
          channelId: live
          projectId: q2chat
